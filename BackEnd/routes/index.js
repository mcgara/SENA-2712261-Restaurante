import * as common from './common.js';
import * as routes from './routes.js';

export const commonRoutes = common;

export const {
  UserRoute,
  FoodRoute,
  FoodCategoryRoute,
  OrderRoute,
  InvoiceRoute
} = routes;
